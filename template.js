export default class template {
	constructor (templateObj) {
		// switch (templateObj) {
		// 	case "prompt":
		// 		this.buttons = [new button('ok', () => )]
		// 		break;
		// 	default:
				this.buttons = templateObj.buttons;
				this.content = templateObj.content;
		// }
	}
}
